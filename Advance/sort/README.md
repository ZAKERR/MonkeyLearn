# sort函数简介

## 对原数组进行排序（修改原数组），返回排序后的数组

## usage

### 默认排序（按照unicode码
```
[3, 1, 10].sort(); 
// 结果：[1, 10, 3] ❌ 不是数值排序！
```

### 自定义排序
```
arr.sort((a, b) => {
  // 返回值决定 a 和 b 的相对顺序
});

< 0	a 排在 b 前面。维持位置不变
> 0	a 排在 b 后面

升序：a - b  
降序：b - a  

```